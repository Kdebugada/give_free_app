<Window x:Class="FileShare.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FileShare"
        mc:Ignorable="d"
        Title="Обмен файлами" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Обмен файлами" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
        
        <Grid Grid.Row="1" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Доступные устройства:" FontSize="16" VerticalAlignment="Center"/>
            <Button Grid.Column="1" x:Name="RefreshButton" Content="Обновить" Padding="10,5" Click="RefreshButton_Click"/>
        </Grid>
        
        <ListView Grid.Row="2" x:Name="DevicesListView" SelectionChanged="DevicesListView_SelectionChanged" Margin="0,0,0,10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="{Binding Name}" FontWeight="Bold"/>
                        <TextBlock Grid.Row="1" Text="{Binding Address}" FontSize="12"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <TextBlock Grid.Row="3" x:Name="SelectedDeviceText" Text="Устройство не выбрано" Margin="0,0,0,10"/>
        
        <Button Grid.Row="4" x:Name="SendFileButton" Content="Отправить файл" Padding="10,5" IsEnabled="False" Click="SendFileButton_Click"/>
        
        <ProgressBar x:Name="ProgressBar" Width="100" Height="20" HorizontalAlignment="Center" VerticalAlignment="Center" IsIndeterminate="True" Visibility="Collapsed"/>
    </Grid>
</Window> 